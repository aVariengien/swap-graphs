from setuptools import setup, find_packages

setup(
    name="swap_graphs",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # any required dependencies
    ],
    # other setup options
)
